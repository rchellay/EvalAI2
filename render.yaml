services:
  - type: web
    name: evalai-backend
    env: python
    buildCommand: |
      cd backend_django
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: |
      cd backend_django && \
      python manage.py migrate && \
      python manage.py create_admin && \
      gunicorn config.wsgi:application
    plan: free
    region: oregon
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: evalai-backend.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: evalai-db
          property: connectionString
      - key: OPENROUTER_API_KEY
        sync: false
      - key: HUGGINGFACE_API_KEY
        sync: false
      - key: GOOGLE_CLOUD_PROJECT_ID
        sync: false
      - key: GOOGLE_CLOUD_API_KEY
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        value: https://evalai-frontend.vercel.app
      - key: LANGUAGE_TOOL_API_URL
        value: https://api.languagetool.org/v2/check
      - key: DJANGO_SUPERUSER_USERNAME
        value: admin
      - key: DJANGO_SUPERUSER_EMAIL
        value: admin@example.com
      - key: DJANGO_SUPERUSER_PASSWORD
        value: admin123

  - type: pserv
    name: evalai-db
    env: postgresql
    plan: free
# Cambio mínimo para forzar despliegue
# Render debe aplicar migraciones sobre la base de datos limpia
# Forzar despliegue tras eliminar tabla conflictiva
